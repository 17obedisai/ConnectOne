# Entorno de producción — usado por `npm run build` (Render + Hostinger)
# El sufijo /api es obligatorio: el backend monta las rutas bajo /api/*
# IMPORTANTE: este es el servicio Render VIVO (con env vars + GEMINI_API_KEY).
# El antiguo connectone-backend.onrender.com está caído (failed deploy) — NO usar.
VITE_API_URL=https://connectone-x7j3.onrender.com/api
